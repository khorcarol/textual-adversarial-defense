on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build project
        run: |
          mkdir -p build
          cd build
          cmake ..
          make
      - name: Run tests
        run: |
          cd build/tests
          ctest --output-on-failure
